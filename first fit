def first_fit(blocks, procs):
    b=blocks[:];alloc=[-1]*len(procs)
    for i,p in enumerate(procs):
        for j,x in enumerate(b):
            if x>=p: alloc[i]=j; b[j]-=p; break
    return alloc

blocks=[100,500,200,300,600]
procs=[212,417,112,426]
a=first_fit(blocks,procs)
print(a)
